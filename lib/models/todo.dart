class Todo {
  String id;
  String title;
  String description;
  bool isChecked;

  Todo({
    required this.id,
    required this.title,
    required this.description,
    required this.isChecked,
  });
}
